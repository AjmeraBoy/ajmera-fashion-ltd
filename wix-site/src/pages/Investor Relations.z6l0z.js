// =========================================================
// INVESTOR RELATIONS PAGE
// =========================================================
// Per instruction: left intentionally BLANK / in its current format.
// Real investor content, financial statements and annual reports
// will be uploaded as PDFs once available — at that point, add a
// "InvestorDocuments" CMS collection (fields: title, file, category,
// publishedDate) and a repeater bound to it, so new PDFs can be
// added from the Wix admin panel without further code changes.
// =========================================================

$w.onReady(function () {
    // Intentionally left blank for now.
});
